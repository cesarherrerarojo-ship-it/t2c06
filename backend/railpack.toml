# Desactivar Railpack completamente
[railpack]
disabled = true

# Forzar uso de Dockerfile
[build]
builder = "dockerfile"
dockerfile = "Dockerfile"

[deploy]
start_command = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 4"