[phases.setup]
nixPkgs = ['python311', 'python311Packages.pip']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Building Python FastAPI application"']

[start]
cmd = 'uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 4'